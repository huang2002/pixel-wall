// @ts-check
import { h } from './common.js';

export const settingsContainer = (
    h('div', {
        id: 'settings-container',
        style: 'display: none',
    }, [
        h('div', {
            id: 'settings-window',
        }, [

        ])
    ])
);
